name: Windows Cloud PC - Anydesk (Optimized)

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Download and Install AnyDesk
        run: |
          Set-ExecutionPolicy Unrestricted -Scope Process -Force
          Write-Host "Baixando o AnyDesk..."
          Invoke-WebRequest -Uri "https://download.anydesk.com/AnyDesk.exe" -OutFile "AnyDesk.exe"
          
          Write-Host "Instalando o AnyDesk..."
          Start-Process -FilePath ".\AnyDesk.exe" -ArgumentList '--install C:\AnyDesk --start-with-win --silent' -Wait
          
          $env:Path += ";C:\AnyDesk"
          [Environment]::SetEnvironmentVariable("Path", $env:Path, [EnvironmentVariableTarget]::Machine)

      - name: Set Random Password and Get AnyDesk ID
        run: |
          $anydeskPath = "C:\AnyDesk\AnyDesk.exe"
          if (Test-Path $anydeskPath) {
              # Inicia o serviço do AnyDesk primeiro para aceitar comandos de configuração
              Start-Service -Name "AnyDesk" -ErrorAction SilentlyContinue
              Start-Process -FilePath $anydeskPath
              Start-Sleep -Seconds 5

              # Gera uma senha aleatoria de 10 caracteres (letras e numeros)
              $randomPassword = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 10 | ForEach-Object {[char]$_})

              # Define a senha de forma limpa via cmd /c para evitar falha no pipe do PowerShell
              cmd /c "echo $randomPassword | `"C:\AnyDesk\AnyDesk.exe`" --set-password"
              
              Start-Sleep -Seconds 5
              
              # Obtém a ID
              $anydeskId = cmd /c "C:\AnyDesk\AnyDesk.exe --get-id"
              
              Write-Host "=========================================="
              Write-Host " ID DO ANYDESK: $anydeskId"
              Write-Host " SENHA GERADA:  $randomPassword"
              Write-Host "=========================================="
          } else {
              Write-Host "Erro: Executável não encontrado."
              exit 1
          }

      - name: Monitor AnyDesk Process
        run: |
          $anydeskPath = "C:\AnyDesk\AnyDesk.exe"
          while ($true) {
            $process = Get-Process -Name "AnyDesk" -ErrorAction SilentlyContinue
            if (-not $process) {
              Write-Host "AnyDesk não está rodando, reiniciando..."
              Start-Process -FilePath $anydeskPath
            }
            Start-Sleep -Seconds 300
          }
